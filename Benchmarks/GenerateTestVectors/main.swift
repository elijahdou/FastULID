import Foundation
import FastULID

// Generate correct test vectors
let vectors: [(String, Data)] = [
    ("All zeros", Data([0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00])),
    ("Max timestamp", Data([0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00])),
    ("All ones", Data([0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF])),
    ("Example", Data([0x01, 0x56, 0x3D, 0xF3, 0x64, 0x81, 0x12, 0x34, 0x56, 0x78, 0x9A, 0xBC, 0xDE, 0xF0, 0x12, 0x34])),
]

print("Correct Test Vectors:\n")
for (name, data) in vectors {
    if let ulid = ULID(ulidData: data) {
        print("// \(name)")
        print("ULIDTestVector(")
        print("    timestamp: \(ulid.timestampMilliseconds),")
        print("    randomBytes: [],  // Not needed, use data directly")
        print("    expectedString: \"\(ulid.ulidString)\",")
        let dataStr = data.map { String(format: "0x%02X", $0) }.joined(separator: ", ")
        print("    expectedData: Data([\(dataStr)])")
        print("),\n")
    }
}

